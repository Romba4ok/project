import 'package:flutter/material.dart';

class ShopScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Это страница магазина', style: TextStyle(fontSize: 24)),
    );
  }
}