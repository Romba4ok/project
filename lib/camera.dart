import 'package:flutter/material.dart';

class CameraScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Это страница камеры', style: TextStyle(fontSize: 24)),
    );
  }
}